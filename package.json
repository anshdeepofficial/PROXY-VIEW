{
  "name": "private-browser",
  "version": "2.0.0",
  "private": true,
  "description": "Privacy-focused remote browser with isolated Playwright Chromium sessions and Vercel WebSocket support.",
  "main": "server/server.js",
  "scripts": {
    "start": "node server/server.js",
    "dev": "node --watch server/server.js",
    "test": "node tests/security.test.js",
    "check": "node --check server/server.js && node --check server/realtime.js && node --check server/browserManager.js && node --check server/proxyManager.js && node --check server/security.js && node --check server/sessionManager.js && node --check public/app.js && node --check api/browser.mjs",
    "build": "npm run check"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@sparticuz/chromium": "149.0.0",
    "dotenv": "^16.6.1",
    "express": "^5.1.0",
    "helmet": "^8.1.0",
    "playwright-core": "1.61.1",
    "ws": "^8.18.3"
  },
  "devDependencies": {
    "playwright": "1.61.1"
  }
}
